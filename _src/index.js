require(['require.config'], function () {

  require([

      // 'apps/css-in-js-test/reactCSS',
      'apps/hello-v-dom'

    ], function (App) {

      // window.onload = App.start();

    });

});

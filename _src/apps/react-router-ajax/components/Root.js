var React = require('react');
var Edit = require('./edit/edit.component');

var Root = React.createClass({

  render: function () {
    return (
      <Edit />
    );
  }

});

module.exports = Root;
